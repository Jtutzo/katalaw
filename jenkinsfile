pipeline {
    agent {
        docker { image 'maven:3-alpine' }
    }
    stages {
        stage('build') {
            steps {
                sh 'mvn clean install'
                args '-v $HOME/.m2:/root/.m2'
            }
        }
    }
}