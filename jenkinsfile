pipeline {
    agent {
        docker { image 'maven:3-alpine' }
    }
    stages {
        stage('build') {
            steps {
                sh 'mv clean install'
                args '-v $HOME/.m2:/root/.m2'
            }
        }
    }
}